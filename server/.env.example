# Copy to server/.env for local dev, and set these in the Deno Deploy dashboard
# for production. Generate a fresh keypair with:  npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com
ALLOW_ORIGIN=*
